fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
# Needed to parse the multipart body of /audio/speech/clone (the voice-cloning
# endpoint) so its `text` field can be normalized alongside the binary ref_audio.
python-multipart>=0.0.18
